<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/sample"/>

    <!--
        이미지의 원래 크기대로 보여지는 것이 아니라 이미지의 크기를 변경해서 보여줘야 할 때 maxWidth, maxHeight속성을 지정함.

        maxWidth, maxHeight속성은 adjustViewBounds속성과 함께 사용해야 이미지의 가로 세로 비율을 유지할 수 있음.
        (가로 세로 비율을 유지해야 하기 때문에 지정한 maxWidth와 maxHeight의 비율이 맞지 않으면 비율을 맞추기 위해 값을 자동으로 조정)

        tint: 이미지 위에 다른 색상을 입힐 때 사용하는 속성
     -->

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="100dp"
        android:maxHeight="100dp"
        android:adjustViewBounds="true"
        android:tint="#800000ff"
        app:srcCompat="@drawable/sample"/>

</LinearLayout>